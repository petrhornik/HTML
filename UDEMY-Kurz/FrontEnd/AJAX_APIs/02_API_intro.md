**API** -> application programming interface

    => komunikace mezi 2 různými softwary

**webAPI**

    => HTTP based komunikace
    -> existují exposenuté EndPointy které reagují na žádosti (requesty) mého kódu
    -> jiné weby, web. aplikace, DB, ...
        -> vždy se odkazuju na URL endpointu
    -> v 90% případů vrací JSON objekt/array

    - poskytovatelem API je třeba coinyep.com, api.binance.com (existuje tier bez apikey) <- kurz krypta
    - twilie.com -> dobré pro automatizaci
    - tip: https://swapi.info/ -> api poskytující info o starwars (gut API tutorial stuff)
