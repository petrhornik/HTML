**OOP (object oriented programming)**

    => organizace kódu, desing a strukturování aplikací rozdělováním kódu na odlišné vzory objektů
        (recepty na objekty...)

    -> vytváření objektů a zacházení se vším jako s objekty