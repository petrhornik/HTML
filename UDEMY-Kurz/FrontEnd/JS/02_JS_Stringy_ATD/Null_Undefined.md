**Undefined**
    = proměnná bez přiřazené hodnody -> značí že hodnota neexistuje
    -> např.: když se snačíme získat z arraye hodnotu s indexem co převyšuhje tu poslední apod.  

**Null**
    = záměrná absence hodnoty -> značí, že existuje zapsaná hodnota, ALE ta hodnota nic neobsahuje