TIP: do fcí. v metodách u arrayů se nám automaticky passují 2 hodnoty v tomto pořadí
    -> value(hodnota), index(pozice položky)

**forEach**
    = funkce uvnitř () u forEach se provede pro KAŽDOU položku v arrayi
        -> podobné jako for loop s podmínkou i < arr_name.length
        -> fci. lze definovat externě či přímo uvnitř ()
            -> uvnitř víc common praktika

    - nahrazuje se forOF prej...

