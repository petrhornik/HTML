**vnoření(nesting) loopů**
    = well... vložím loop do jiného loopu  
        -> vnořený loop se bude po jeho urč. dobu provádět při každém opakování "parent" loopu

    - např.: pokud mám vnořený loop co se provede 10x v loopu co se provede 5x tak se vnořený loop provede celkem 50x

    - viz.: 03_nestingLoops

    - při nestingu by měla initialExpression být jinné písmeno pro přehlednost
      -> když první bude i, tak druhá např. j apod. 

    - 1 z použítí je např. když pracujeme s vnořenými arrayi (array obsahující arraye)
    - 