**returnování fcí.**
    = do returnu fce. specifikujeme další fci. BEZ NÁZVU, která bude jako výstup
      - returnovvanou fci. zapíšu např. do nějaké proměnné, ze které tuto fci následně mohu spustit
    - lze vracet fci. jako výstup např. if else rozhodováním
    - volání fce napojíme na nějakou prom. a tu následně pomocí () spustíme
      -> that's actually it...

    - builder fce. (factory function)
        = fce. co vytváří jinné fce, které následně returnuje
          - z builder fce. se nám vytvoří a vrátí ta "vyrobená", dle toho co je specifikováno v builderu
        
        - šetří čas a kód je kratší 