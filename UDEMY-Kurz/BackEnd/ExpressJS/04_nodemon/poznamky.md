**nodemon**

    => cmd tool pro automatické restartovaní nodeJS serveru při změně kódu

    -> instalovat globálně -> npm install -g nodemon
        -> lze i lokálně ale proč?
    
    -> start nodemon naz_souboru    ->    nodemon index.js 