**File System Module**

    => manipulace se soubory na zařízení

    - existuje synchnní a asynchronní verze

    -> import do proměnné pomocí require -> const fs = require('node:fs');

**metody** -> existuje vždy sync a async varianta

    mkdir -> vytvoření složky
    writeFile -> vytvoří soubor s daty v určené lokalitě