**NodeJS**

    => JS runtime, schopnost rozjet JS mimo prohlížeč

    - velmi popupární s velkým množstvím community packages

   1) využití

    -> web servery, nativní aplikace, cmd tooly, hry, ...