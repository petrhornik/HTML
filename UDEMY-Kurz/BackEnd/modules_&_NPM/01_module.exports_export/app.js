const math = require("./math"); // starý způsob importu -> mělo by se používat import naz_prom from naz_souboru (import math from './math') apod.
console.log(math);

console.log(math.square(5)); // funkce přivolaná z mého math.js
