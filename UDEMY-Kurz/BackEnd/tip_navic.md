BE -> pro tvoření stránek pomocí kódu (dynamicky)

**UUID npm package**

  => při zavolání vytvoří vždy unikátní identifikátor typu RFC9562
        - chci konkrétně v4
        - https://www.npmjs.com/package/uuid
        - dobré pro manipulaci a indexaci velkého množství položek (primárně pro DB a CRUD content)