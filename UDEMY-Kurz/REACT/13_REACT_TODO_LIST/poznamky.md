TIPY/TRIKY


**returnování v mapě**
    = 2 způsoby
        -1) .map((val) => {return <p>{val}</p>})
        -2) .map ((val) =>(<p>{val}</p>))

**generování random uuid bez NPM package UUID**
    = přímo prohlížeč obsahuje generátor
    - crypto.randomUUID() <- generuje náhodný řetězec pro použití jako ID

**Vyhledávání MUI ikon**
    = databáze: https://mui.com/material-ui/material-icons/

**smazání default html stylů pomocí MUI**
    = komponent CSSBaseline
    - reset včech stylů(např.: defaultní odsazení od body apod.)

**MUI icony a JS/TS/HTML**
    = MUI ikony se dají použít i u klas. JS/TS/HTML -> u html na <Button> se dají zobrazit jako actual ikona :DDDD

**STYLIZACE**
    = MUI komponentám lze nastavit className="" -> funkcionalita CSS zatím netestována :(

REACT...TBD v jýnám kurzu...